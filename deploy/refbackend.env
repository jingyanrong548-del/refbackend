# refbackend systemd 环境变量
# RPPREFIX: REFPROP 目录，需含 librefprop.so 和 FLUIDS（见 DEPLOY.md）
RPPREFIX=/www/refprop/Refprop10.0
# WINE_REFPROP_URL: 8002 后端 URL。留空则直接用 ctREFPROP（需 librefprop.so）
WINE_REFPROP_URL=
